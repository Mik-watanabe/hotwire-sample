<div id="<%= dom_id @client %>">
  <p>
    <strong>名前:</strong>
    <%= @client.name %>
  </p>

  <p>
    <strong>携帯番号:</strong>
    <%= @client.tel %>
  </p>
</div>

<div>
  <%= link_to "編集", edit_client_path(@client) %> |
  <%= link_to "戻る", clients_path %>
  <%= button_to "削除", @client, method: :delete %>
</div>
